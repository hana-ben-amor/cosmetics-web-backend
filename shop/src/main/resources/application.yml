spring:
  application:
    name:
      shop
  datasource:
    url: jdbc:mysql://localhost:3306/cosmetics_db?useSSL=false&serverTimezone=UTC
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true